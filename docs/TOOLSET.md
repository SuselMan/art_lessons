# Toolset Specification

Дополняет `docs/MANIFESTO.md` — конкретный набор инструментов, оправдываемый
принципом 3 ("только то, что нужно для рисунка карандашом") и анти-целями
манифеста. Состояние сверено с кодом на 2026-07-16.

## Принцип отбора

Инструмент попадает в тулсет, только если служит одному из:
1. Собственно нанесению графита/цвета на холст.
2. Классическим вспомогательным техникам академического рисунка (пропорции,
   перспектива, измерение) — тем, что учитель физически показывал бы мелом
   или линейкой на бумаге.
3. Объяснению/референсу учителем (не отдельный "режим", а применение тех же
   инструментов рисования и импорта изображений).

Всё, что не попадает ни в одну категорию — по умолчанию не в тулсете, даже
если технически просто добавить (см. "Явно вне тулсета" ниже).

## Текущее состояние (реализовано)

### Рисующие инструменты (`ToolType` в `packages/shared/src/index.ts`)

| Инструмент | Статус | Детали |
|---|---|---|
| **Pencil (графит)** | ✅ Реализован | 14 градаций твёрдости 6H–6B (`engine/src/pencilPresets.ts`), чувствительность к нажиму/наклону/скорости через dab-систему, произвольный цвет (`StrokeOperation.color` + `ColorPicker`) — сейчас один инструмент на графит и цвет; см. "Пересмотр: два pencil-инструмента" ниже про разделение на отдельный Color pencil-слот. В левом тулбаре и `FloatingToolPanel` |
| **Eraser** | ✅ Реализован | В тулбаре и `FloatingToolPanel`, хоткей `e` |
| **Smudge (растушёвка)** | ⚠️ Только заглушка типа | `'smudge'` есть в `ToolType` и в `DEFAULT_HOTKEYS` (`r`), но **не реализована нигде в движке** (нет упоминаний в `engine/`) и хоткей `r` в реальности занят под сброс угла холста, не под этот инструмент. Требует полноценной разработки — алгоритм смешивания уже нанесённого графита между дабами, вероятно новый шейдер-пасс — не просто добавление кнопки |

### Цвет

| Инструмент | Статус |
|---|---|
| **Color picker (HSV)** | ✅ Реализован (`components/ColorPicker`) |
| **Eyedropper** | ✅ Реализован — берёт цвет с холста, открывает ColorPicker. Одноразовый режим, не `ToolType`, не порождает Operation |

### Вспомогательные инструменты (не рисуют, не порождают Operation — кроме ruler-снаппинга, который влияет на геометрию штриха)

| Инструмент | Статус | Детали |
|---|---|---|
| **Ruler** | ✅ Реализован | Перетаскиваемая прямая направляющая; штрих карандаша, проведённый вдоль неё, снэпится к линии (`engine/src/rulerSnap.ts`) |
| **Measure** | ✅ Реализован | Одноразовое измерение расстояния/угла A→B, не рисует |
| **Construction grid** | ✅ Реализован | Пассивный оверлей-тумблер (сетка для пропорций) |
| **Layer transform** | ✅ Реализован | Перемещение/масштаб/поворот содержимого слоя (гизмо), записывается как `LayerTransformOperation` |

### Референс и структура

| Инструмент | Статус |
|---|---|
| **Импорт референсного изображения** | ✅ Реализован (`ImageImportOperation`, UI в `LayerPanel`) — учитель кладёт фото/референс на отдельный слой |
| **Слои** | ✅ Реализован — добавление/удаление/перемещение/переименование/opacity/visibility/merge, папки в один уровень, фоновый слой неудаляем/неперемещаем |

## Решения по открытым вопросам этой сессии

1. ~~Цветные карандаши = существующий pencil-инструмент + произвольный
   цвет, без отдельной "восковой" физики.~~ **Пересмотрено 2026-07-18** —
   см. "Пересмотр: два pencil-инструмента" ниже.
2. **Растушёвка — приоритет "сделать в первую очередь", но это полноценная
   инженерная задача**, не мелкая UI-доработка (см. таблицу выше). Нужен
   отдельный тикет с постановкой алгоритма, прежде чем начинать реализацию.
3. **Механизм асинхронной обратной связи по домашке (цветной штрих поверх
   работы vs. отдельные текстовые комментарии) — открытый вопрос,
   сознательно не решается сейчас.** Будет решаться вместе с UX-дизайном
   разбора домашних заданий.

## Пересмотр: два pencil-инструмента (2026-07-18, issue #188)

Решение 1 выше заменено. Вместо одного pencil-инструмента с произвольным
цветом — **два независимых инструмента в тулбаре**, оба технически
`tool: 'pencil'` (никаких изменений в `Operation`/движке/протоколе не
требуется, только клиентский UI и раздельное локальное состояние):

- **Pencil (графит)** — как сейчас: шкала твёрдости 6H–6B, свой размер.
- **Color pencil (цветной карандаш)** — та же шкала твёрдости 6H–6B
  (переиспользуется тот же grade-селектор, не новый UI), но **своё
  независимое состояние** (градация, размер, цвет) — тот же приём, что уже
  используется для размера ластика отдельно от размера карандаша (`pencilCfg`
  / `eraserCfg` в `Room/index.tsx`; здесь просто добавляется третий
  независимый cfg).

Причины (обе явно проговорены Ilya):
1. У графитового pencil сейчас общий с "цветным" цвет — один раз взяв цвет
   для аннотации, приходится вручную искать обратно графитовый серый. Два
   независимых состояния убирают эту проблему полностью.
2. Учителю нужно одновременно держать "чем показываю технику" и "чем
   отмечаю ошибки", не переключая цвет туда-обратно — как два реальных
   карандаша в руке, а не один перекрашиваемый.

Не решает: реальные цветные карандаши не имеют шкалы твёрдости 6H–6B —
это компромисс ради переиспользования уже существующего UI-компонента и
единообразия с тем, как ластик уже устроен, а не попытка физической
точности для этого конкретного инструмента.

## Целевой набор инструментов (полная спека)

| Категория | Инструмент | Статус |
|---|---|---|
| Рисование | Pencil (графит, 6H–6B) | Готово |
| Рисование | Eraser | Готово |
| Рисование | Smudge/растушёвка | Запланировано — нужен тикет с постановкой алгоритма |
| Рисование | Color pencil (второй pencil-слот, своё состояние) | Запланировано — issue #188, см. "Пересмотр: два pencil-инструмента"; чисто UI-задача, без изменений протокола |
| Цвет | Color picker | Готово |
| Цвет | Eyedropper | Готово |
| Вспомогательные | Ruler (прямая направляющая) | Готово |
| Вспомогательные | Measure (расстояние/угол) | Готово |
| Вспомогательные | Construction grid | Готово |
| Вспомогательные | Perspective/ellipse guide (точки схода, эллипсы для цилиндров) | Не начато — кандидат для отдельной проработки, см. открытые вопросы |
| Референс | Импорт референсного изображения | Готово |
| Слои | Полный CRUD, merge, transform, папки, фон | Готово |
| Обратная связь | Аннотация/комментарии к домашке | Открытый вопрос, не в MVP-скоупе спеки |

## Явно вне тулсета (анти-цели, см. `docs/MANIFESTO.md`)

- Маски слоёв
- Анимация (onion skin, таймлайн, таймлапс-экспорт)
- Фильтры/эффекты, blend-моды за пределами базовой opacity
- AI-дорисовка, авто-шейдинг, генерация изображений
- Векторные заливаемые фигуры (прямоугольник/эллипс как shape-инструмент —
  не путать с perspective/ellipse *guide* из открытых вопросов, это разные
  вещи: guide ничего не заливает, только направляет руку)
- Текст как элемент рисунка на холсте
- Заливка (fill bucket), лассо-выделение области
- Публикация/шеринг работ наружу, соцфункции

## Открытые вопросы (не блокируют текущую спеку)

- Механизм асинхронной обратной связи по домашке (см. решение 3 выше).
- Perspective/ellipse guide — расширять ли ruler/grid до полного набора
  pedagogical aids, или считать, что бумажная сетка их не заменяет и это
  нужно отдельно продумывать (как и планировал Ilya отдельной задачей).
- Per-student холсты в групповом уроке (v2, см. `docs/MANIFESTO.md`) потребуют
  собственного пересмотра тулсета — например, нужен ли учителю инструмент
  "выделить/показать всем холст одного ученика".
